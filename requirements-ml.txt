# Optional ML stack for src/predict_model.
# main.py runs with the baseline forecaster when these are not installed.
-r requirements.txt

catboost>=1.2
scikit-learn>=1.4
scipy>=1.12
holidays>=0.45
optuna>=3.6
polars>=0.20
